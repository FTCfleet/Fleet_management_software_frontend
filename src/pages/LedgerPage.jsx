import { React } from "react"
// import { Outlet } from "react-router-dom"

export default function LedgerPage() {
    return (
        <div className="app">
                <p>This is Ledger</p>
        </div>
    );
}